#include "shell.h"

int main(void)

{
	char command[120];
	while (true)
	{
	display_prompt();
	void read_command(char *command, size_t size);
	void execute_command(const char *command);

	}
	return(0);
}

